databases:
  - name: Money-Mosaic
    databaseName: Money-Mosaic
    user: evans

services:
  - type: web
    name: Money-Mosaic
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: MoneyMosaic
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false